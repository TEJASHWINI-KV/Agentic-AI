1] What is the difference between a ToolCallingAgent and a CodeAgent?

2] Why does the LLM output JSON instead of just calling the function directly?

3] What happens if the LLM generates malformed JSON?

4] When would you choose a ToolCallingAgent over a CodeAgent in production?

5] How does the LLM know which tools are available?

6] What is a tool in the context of AI agents?

7] Why do tools need descriptions if the LLM is smart enough to figure it out?

8] What's the difference between a tool and a function?

9] How does smolagents convert my function into a tool the LLM can use?

10] What happens if two tools have similar descriptions?

11] Can a tool call another tool?

12] What's the security risk with tools, and how do you mitigate it?
